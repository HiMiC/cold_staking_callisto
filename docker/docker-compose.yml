version: "3"

services:
  gethnode:
    container_name: gethnode
    build: ./gethnode
    working_dir: /root
    command: /usr/local/bin/geth --syncmode "full" --rpc --rpcaddr 0.0.0.0 --rpcport 8545 -rpcapi="db,eth,net,web3,personal" --rpccorsdomain="" --rpcvhosts "*" --wsaddr 0.0.0.0 --wsport 8546 --wsapi="db,eth,net,web3,personal" --verbosity 9
    volumes:
      - ./counteiner_data/geth:/root/.callisto
    networks:
      - front
    ports:
      - "8545:8545"
      - "8546:8546"
      - "30303:30303"
      - "30303:30303/udp"

  db:
    build: mysql/
    container_name: "db"
    environment:
      MYSQL_DATABASE: 'clo_statistic'
      MYSQL_USER: 'clo_statistic'
      MYSQL_PASSWORD: 'clo_statistic'
      MYSQL_ROOT_PASSWORD: 'password'
    networks:
      - front
    ports:
      - "3311:3306"

  web:
    build: web/
    container_name: "web"
    entrypoint: /code/docker-entrypoint.sh
    volumes:
      - ./web/code/:/code
    ports:
      - 8000:8000
    depends_on:
      - db
      - gethnode
    networks:
      - front

networks:
  front: